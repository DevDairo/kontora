services:
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: kontora_pos_backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8080}:8080"

